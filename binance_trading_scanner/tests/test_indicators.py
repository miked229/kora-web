"""Indicators tests — scheduled for Phase 2.

Placeholder so the suite stays green until the Phase 2 implementation lands.
Real cases (per the spec) will cover computation correctness and edge cases:
zero volume, missing/duplicate candles, invalid inputs, extreme values.
"""
import pytest

pytestmark = pytest.mark.skip(reason="indicators implemented in Phase 2")


def test_placeholder():
    assert True
