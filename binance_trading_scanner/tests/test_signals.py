"""Signals tests — scheduled for Phase 3.

Placeholder so the suite stays green until the Phase 3 implementation lands.
Real cases (per the spec) will cover computation correctness and edge cases:
zero volume, missing/duplicate candles, invalid inputs, extreme values.
"""
import pytest

pytestmark = pytest.mark.skip(reason="signals implemented in Phase 3")


def test_placeholder():
    assert True
